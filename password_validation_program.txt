#include <stdio.h>
#include <string.h>

int main() {
    char password[20];

    printf("Enter password: ");
    scanf("%s", password);

    if(strlen(password) >= 8)
        printf("Password is Strong\n");
    else
        printf("Password is Weak\n");

    return 0;
}
